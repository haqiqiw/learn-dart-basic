void main(List<String> arguments) {
  var first = 10;
  var second = 3;

  print(first + second);
  print(first - second);
  print(first * second);
  print(first / second); // with double result
  print(first ~/ second); // with int result
  print(first % second);
  print(-first); // set to negative
}